[package]
name = "axionvera-monitoring"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "22.0.1111"
axionvera-events = { path = "../events" }

[dev-dependencies]
soroban-sdk = { version = "22.0.1111", features = ["testutils"] }
